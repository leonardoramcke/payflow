// Ponto de entrada da aplicação React.
// Nas próximas etapas vamos:
// 1. Configurar o React Router (pages/Login, pages/Dashboard, etc.)
// 2. Envolver a árvore com o QueryClientProvider (@tanstack/react-query)
// 3. Criar o layout base (Sidebar + Navbar) em components/

function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'sans-serif' }}>
      <h1>PayFlow</h1>
      <p>Projeto em construção — próximo passo: configurar o roteamento.</p>
    </div>
  );
}

export default App;
